query IssueParentGetById($issueId: ID!) {
  node(id: $issueId) {
    __typename
    ... on Issue {
      parent {
        id
        number
        title
        url
        state
        repository {
          owner {
            login
          }
          name
        }
      }
    }
  }
}
